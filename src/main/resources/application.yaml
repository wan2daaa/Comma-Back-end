server:
  port: 8080

#logging
logging:
  level:
    org:
      hibernate:
        sql: debug
        type.descriptor.sql: trace
      springframework.web: debug
    jakarta: debug
spring:
  profiles:
    include: oauth
